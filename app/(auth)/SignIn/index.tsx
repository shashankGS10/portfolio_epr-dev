import React from 'react'

type SignInProps = {
    permission:boolean;
}

const SignInWrapper:React.FC<SignInProps> = ({}) => {
  return (
    <div>
        <span>SignInWrapper</span>

    </div>
  )
}

export default SignInWrapper